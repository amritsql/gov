apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: nginx-monitor
  namespace: fe
spec:
  selector:
    matchLabels:
      app: nginx
  endpoints:
  - port: http
    path: /metrics

